import { NotificationI } from "../common/interfaces";
export declare class NotificationService {
    private headers;
    private baseUrl;
    constructor(baseUrl: string, headers: any);
    /**
     * Fetches an item by its ID.
     * @param {string} itemType - The type of the item. (table name)
     * @param {string} id - The ID of the item.
     * @returns {Promise<any>} - A promise that resolves to the item data.
     */
    createNotification(notification: NotificationI): Promise<any>;
}
